export enum GameForgeParseType {
  Promotions = 'promotions',
}

export enum GameForgeParseSourceType {
  Url = 'url',
  File = 'file',
}

export enum GameForgeSortColumn {
  VersionId = 'versionId',
  CreatedAt = 'createdAt',
  GameVersionReleasedAt = 'gameVersion.releasedAt',
}
